const init_state = {
    username: "",
    email: "",
    id: 0,
    password: "",
    verifiedStatus: false,
    fullname: ""
}

export default ( state = init_state, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}